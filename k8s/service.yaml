apiVersion: v1
kind: Service
metadata:
  name: smart-inventory-service
spec:
  type: LoadBalancer
  selector:
    app: smart-inventory-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000